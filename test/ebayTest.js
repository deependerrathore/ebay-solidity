const Ebay = artifacts.require("Ebay");
const { expectRevert } = require("@openzeppelin/test-helpers");

contract("test", () => {
  it("check what's in the accounts ", async () => {
    console.log("Deep");
  });
});
